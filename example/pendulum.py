"""This module is an example usage of the `cartesian` package.

It evolves a function describing the movement of a pendulum without small angle
approximation and with air resistance in terms of angular position Theta(time).
The function is then graphed using `pyplot` and displayed.
"""
from matplotlib import pyplot as plt

x = [1, 2, 3]
y = [4, 5, 6]

plt.plot(x, y)
plt.show()
